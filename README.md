# PiterovaNHW03

A simple Python project uploaded from the assignment.

## Files
- `PiterovaNHW03.py`

## How to run

```bash
# 1) Create and activate a virtual environment (recommended)
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# 2) Install dependencies
pip install -r requirements.txt

# 3) Run the main script (adjust filename as needed)
python YOUR_MAIN_FILE.py
```

## Notes

- Replace `YOUR_MAIN_FILE.py` with the actual entry point of your program.
- If there is input data required, place it in a folder called `data/` and update the script accordingly.
- This README was auto-generated to help publish on GitHub.